/*
	handles change over time
*/

function Modulator(params) {

	let value = params.value ?? 0;
	let min = params.min ?? 0;
	let max = params.max ?? 1;
	let step = params.step ?? 1;
	let chance = params.chance ?? 0.5;

	let isWalker = params.isWalker ?? true; // return stepped values
	let isInt = params.isInt ?? false; // return rounded

	if (params.minMod) {

	}

	if (params.maxMod) {

	}


}